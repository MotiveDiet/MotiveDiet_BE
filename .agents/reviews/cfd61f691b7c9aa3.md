---
code_hash: cfd61f691b7c9aa3
stage: code
at: 2026-07-18T12:08:32+0900
reviewer: codex
---

# Codex 교차 검증 — code

이 기록은 코드 해시 `cfd61f691b7c9aa3` 를 검증한 것이다. 이후 코드가 바뀌면 게이트가 다시 막는다.

## Codex 출력 (원문)

**Findings**

1. [check-review.py](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/check-review.py:90) 해시가 파일 실행 권한을 보지 않아 거짓 통과가 가능합니다.

재현 시나리오:
- 입력: `.agents/scripts/sync-mcp.sh` 내용을 그대로 두고 실행 권한만 `100755 -> 100644`로 변경.
- 기대: [.github/workflows/ci.yml](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.github/workflows/ci.yml:70)의 `run: .agents/scripts/sync-mcp.sh --check`가 깨지므로 새 Codex 리뷰가 필요.
- 실제: [check-review.py](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/check-review.py:90)는 경로+내용만 해시해서 기존 `.agents/reviews/<hash>.md`가 그대로 인정될 수 있음.

수정 방향: `code_hash()`에 파일 mode를 포함해야 합니다. 최소한 `100755/100644/120000`을 path와 content hash 사이에 넣어야 합니다.

2. [check-review.py](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/check-review.py:68), [quality-gate.py](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/quality-gate.py:61), [codex-review.sh](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/codex-review.sh:48) 모두 경로 처리가 newline/whitespace-safe가 아닙니다.

재현:
- 입력: untracked `.github/workflows/space name.yml`
- 기대: 코드 경로 변경으로 인식되고 리뷰 해시에 포함, 프롬프트에도 변경 파일로 주입.
- 실제 확인: `git ls-files --others --exclude-standard .github/workflows` 출력은 `.github/workflows/space name.yml\n`인데 Python의 `.split()` 결과가 `['.github/workflows/space', 'name.yml']`가 되어 둘 다 존재하지 않는 파일로 처리됩니다.

영향:
- 신규 코드 파일이 해시에서 빠져 기존 리뷰 기록이 재사용될 수 있음.
- `quality-gate.py changed_files()`도 같은 이유로 파일을 잘못 쪼갭니다.
- `codex-review.sh`는 공백은 일부 버티지만 newline 파일명은 구조적으로 깨지고, 표시도 escape 없이 프롬프트에 들어갑니다.

수정 방향: Git 파일 목록은 `-z`로 받고 NUL split해야 합니다. Bash 3.2 호환까지 보려면 `codex-review.sh`에서는 command substitution에 NUL을 담지 말고 Python helper 또는 `while IFS= read -r -d ''` 입력으로 처리해야 합니다.

3. [sync-mcp.sh](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/sync-mcp.sh:82)를 공유하는 MCP 로컬 게이트가 커밋 대상이 아닌 홈 디렉터리 상태 때문에 거짓 실패할 수 있습니다.

재현:
```bash
mkdir -p /private/tmp/codex-empty
printf '' > /private/tmp/codex-empty/config.toml
CODEX_HOME=/private/tmp/codex-empty .agents/scripts/sync-mcp.sh --check
```

실제 출력:
```text
.mcp.json: 최신
~/.codex/config.toml: 동기화 필요
```
exit code는 `1`.

기대 vs 실제:
- 기대: repo 산출물인 `.agents/mcp/servers.json` ↔ `.mcp.json`이 최신이면 머지 게이트 관점에서는 통과.
- 실제: 로컬 `CODEX_HOME/config.toml` 존재 여부와 내용에 따라 같은 repo diff가 실패하거나 통과합니다. CI는 이 파일이 없어서 통과할 수 있어 로컬/CI 판정이 갈립니다.

**Run Trigger Regression**

`run()`이 코드 변경 또는 MCP 파일 변경에서만 도는 변경 자체로 인해 기존에 잡히던 Java 컴파일/테스트 결함이 빠지는 회귀는 확인하지 못했습니다. 다만 위 1번 때문에 “CI 실행면의 mode-only 변경”은 코드 변경으로 감지되어도 리뷰 해시가 바뀌지 않아 기존 리뷰로 통과할 수 있습니다.

**Verification**

실행한 확인:
- `python3 .agents/scripts/quality-gate.py --report` → `6/7`, Codex 리뷰 기록 불일치로 실패.
- `python3 .agents/scripts/check-review.py --verify` → 현재 코드 해시 `cfd61f691b7c9aa3`에 맞는 리뷰 기록 없음.
- 공백 파일명 재현 완료.
- MCP `CODEX_HOME` 거짓 실패 재현 완료.

수정은 시도했지만 이 세션에서 `.agents` 아래 파일 쓰기가 `Operation not permitted`로 막혀 적용하지 못했습니다.