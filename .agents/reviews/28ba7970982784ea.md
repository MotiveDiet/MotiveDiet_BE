---
code_hash: 28ba7970982784ea
stage: code
at: 2026-07-20T19:33:46+0900
reviewer: codex
---

# Codex 교차 검증 — code

이 기록은 코드 해시 `28ba7970982784ea` 를 검증한 것이다. 이후 코드가 바뀌면 게이트가 다시 막는다.

## Codex 출력 (원문)

검토 결과: 발견된 결함은 없습니다. 수정 없이 승인 가능합니다.

재현 결과:

1. `client_id` / `state`
   - 입력: `12345-abc.apps.googleusercontent.com`, `AbC_-09xyz`
   - 실제 출력과 디코딩 결과가 원본과 동일했습니다.
   - Base64 URL-safe 문자 `-`, `_`가 변형되지 않아 쿠키와의 상수시간 비교도 유지됩니다.

2. `redirect_uri`
   - 입력: `https://api.example.com/api/oauth2/callback/google`
   - 실제 출력에서도 `:`, `/`가 그대로 유지됐습니다.
   - Google 등록 URI 매칭을 깨뜨릴 형태로 `%3A`, `%2F` 변환되지 않았습니다.

3. `scope`
   - 두 scope 사이 공백이 실제로 `%20`으로 출력됐습니다.
   - 디코딩하면 원래 scope 문자열과 정확히 일치합니다.

4. 인증 신원 및 CSRF 경계
   - diff는 `getGoogleLoginUrl()`의 `.encode()` 추가 한 줄뿐입니다.
   - `findByGoogleId` 우선 조회와 제한된 `findByEmail` 마이그레이션 경로를 건드리지 않습니다.
   - state 생성·쿠키 저장·콜백 검증·상수시간 비교 경로도 변경하지 않습니다.

검증:

- `git diff --check`: 통과
- `./gradlew test --offline`: 통과
- 전체 변경 범위: `AuthService.java` 1줄 추가 בלבד