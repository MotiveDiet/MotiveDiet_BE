---
code_hash: 7b4cce9da46b1783
stage: code
at: 2026-07-20T16:20:42+0900
reviewer: codex
---

# Codex 교차 검증 — code

이 기록은 코드 해시 `7b4cce9da46b1783` 를 검증한 것이다. 이후 코드가 바뀌면 게이트가 다시 막는다.

## Codex 출력 (원문)

- [P1] D-day 경로의 빈 `motiveComboText`가 생성 실패로 처리되지 않습니다. [OpenAiClient.java:109](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/src/main/java/com/example/motivediet_be/service/OpenAiClient.java:109)는 `text`만 검사하고, [CoachMessageService.java:55](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/src/main/java/com/example/motivediet_be/service/CoachMessageService.java:55)는 빈 콤보를 조용히 생략합니다. 예를 들어 유효한 동기가 D-10이고 모델이 스키마상 허용되는 `{"text":"치킨 또 먹네","motiveComboText":"   "}`를 반환하면, API는 정상 `coachMessage`를 반환하지만 `motiveCombo`가 없습니다. 이는 “유효한 동기 신호가 D-14 이내면 `motiveCombo`가 존재한다”는 계약과 어긋납니다. `daysUntil != null`일 때 `motiveComboText`도 `hasText`가 아니면 `OpenAiClient`에서 예외를 던져 전체 메시지를 graceful `null` 처리해야 합니다.

그 외 이번 빈 문자열 수정에서 추가 회귀는 찾지 못했습니다. `./gradlew test --offline --rerun-tasks`와 `git diff --check`도 통과했습니다.