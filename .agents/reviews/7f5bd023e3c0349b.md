---
code_hash: 7f5bd023e3c0349b
stage: code
at: 2026-07-18T12:16:23+0900
reviewer: codex
---

# Codex 교차 검증 — code

이 기록은 코드 해시 `7f5bd023e3c0349b` 를 검증한 것이다. 이후 코드가 바뀌면 게이트가 다시 막는다.

## Codex 출력 (원문)

새 결함은 찾지 못했습니다.

확인한 재현 결과:

- `.mcp.json`을 실제로 깨뜨린 경우: `mcpServers` 키를 바꾸고 `CODEX_HOME=/nonexistent bash .agents/scripts/sync-mcp.sh --check` 실행 → `.mcp.json: 동기화 필요`, exit `1`.
- `servers.json` 정본만 바꿔 `.mcp.json`과 어긋나게 한 경우: 정본의 `args`에 값을 추가하고 같은 명령 실행 → `.mcp.json: 동기화 필요`, exit `1`.
- 정본 없음 분기: `servers.json` 제거 후 실행 → `정본이 없다: ...servers.json`, exit `1`. `CODEX_HOME` 분기까지 가지 않습니다.
- Codex config 분기: `.mcp.json`이 최신이고 `CODEX_HOME=/nonexistent`이면 `Codex 설정 없음(/nonexistent/config.toml) — 건너뜀`, exit `0`. 존재하는 빈 `config.toml`을 가리키면 기존처럼 `~/.codex/config.toml: 동기화 필요`, exit `1`이라 우회 의도가 그 분기에만 적용됩니다.
- env 전달: `dict(os.environ, CODEX_HOME="/nonexistent")` 상태에서 `PATH`는 유지됐고, `bash`는 `/bin/bash`, `python3`는 `/usr/local/bin/python3`로 탐색됐습니다.

라인 대조상 `.agents/scripts/sync-mcp.sh`는 정본 확인과 Claude `.mcp.json` 비교를 먼저 수행하고([sync-mcp.sh](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/sync-mcp.sh:23), [sync-mcp.sh](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/sync-mcp.sh:32)), Codex config 존재 여부는 그 뒤([sync-mcp.sh](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/sync-mcp.sh:44))라서 이번 `CODEX_HOME` 주입([quality-gate.py](/Users/seok_hwan_/MotiveDiet/MotiveDiet/MotiveDiet_BE/.agents/scripts/quality-gate.py:211))이 커밋 산출물 비교를 우회하는 경로는 재현되지 않았습니다.

참고로 `.mcp.json` 끝에 빈 줄만 추가한 차이는 `$(cat "$CLAUDE_OUT")`의 trailing newline 제거 때문에 통과했습니다. 다만 이건 이번 `CODEX_HOME` 델타로 생긴 경로가 아니고, 서버 정의 불일치 재현과는 별개입니다.